/// <reference types="node" />
export declare class Parquet {
    static toParquet<T>(options: {
        schema: Record<string, any>;
        data: Record<any, any>[];
        fileName?: string;
        rowFunc?: (value: any, key?: string, row?: T) => any;
    }): Promise<{
        path: string;
        file: Buffer;
    }>;
}
