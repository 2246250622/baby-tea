/// <reference types="multer" />
/// <reference types="node" />
export declare class File {
    static unlink(filePath?: string, dirPath?: string): Promise<void>;
    static createTempFileFromString(options: {
        fileName: string;
        mimetype: string;
        content: string;
    }): Promise<{
        filePath: string;
        file: Express.Multer.File;
        dirPath: string;
    }>;
    static createTempFilePath(fileName?: string): Promise<{
        filePath: string;
        dirPath: string;
        fileName: string;
    }>;
    static md5Checksum(file: Buffer): string;
    static createFileFromBase64(base64: string, filename: string): Promise<Express.Multer.File>;
    static downloadFile(url: string): Promise<Express.Multer.File>;
    static getMimeType(fileName: string): string;
    static exists(filePath: string): boolean;
    static read(filePath: string): Promise<string>;
}
