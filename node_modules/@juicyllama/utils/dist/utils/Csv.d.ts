/// <reference types="multer" />
export declare class Csv {
    static parseCsvFile(file: Express.Multer.File, mappers?: {
        [key: string]: string;
    }): Promise<any[]>;
    static createTempCSVFileFromString(content: string): Promise<{
        filePath: string;
        csv_file: Express.Multer.File;
        dirPath: string;
    }>;
}
