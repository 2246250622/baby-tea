import { INestApplication, Type } from '@nestjs/common';
import { AbstractHttpAdapter } from '@nestjs/core';
export declare type Adapter = Type<AbstractHttpAdapter>;
export declare function createServer(controller: Type<any>, Adapter: Adapter): Promise<INestApplication>;
